# Local notes

Rename to `CLAUDE.local.md` and add it to `.gitignore`.

Allowed:

- local non-production URLs;
- local Docker profile;
- non-secret machine commands.

Never place credentials or employer material here.

# Svelte and SvelteKit

- Follow the installed Svelte and SvelteKit reactivity model.
- Keep load, action and client code boundaries explicit.
- Prefer platform forms and progressive enhancement.
- Avoid unnecessary client hydration.
- Treat stores as shared state only when ownership requires them.
- Measure bundle, server rendering and browser work.
- Test no-JavaScript behavior where the product requires it.

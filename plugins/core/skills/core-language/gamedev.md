# Game development

- Define frame, memory, loading and network budgets.
- Profile frame time before optimizing architecture.
- Avoid per-frame allocation and expensive scene traversal in hot loops.
- Keep simulation deterministic where networking or replay requires it.
- Use ECS only when data-oriented scale justifies it.
- Test asset pipelines, save compatibility and platform input.
- Measure CPU, GPU, memory and streaming separately.

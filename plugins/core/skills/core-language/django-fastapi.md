# Django and FastAPI

- Respect sync versus async boundaries and ORM behavior.
- Validate request and response contracts.
- Avoid N+1 queries and implicit lazy access in serialization.
- Keep business behavior outside oversized route handlers.
- Test authentication, permissions and transaction behavior.
- Use background work only with durable ownership and retries.
- Profile under production server configuration.
